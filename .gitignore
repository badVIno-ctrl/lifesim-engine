# Сборка и зависимости в репозитории не живут: dist собирается командой,
# node_modules ставится командой. В истории они только мешают читать diff.
node_modules/
dist/
dev-dist/

# Локальные партии CLI и служебные каталоги хранилищ.
.sim-cli/
.saves/

# Личное окружение
.env
.env.local
*.log
